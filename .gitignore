.DS_Store

# Secrets and local state
.env
.env.local
*.pem
*.key
.data/
# Anchored to the repo root on purpose. Unanchored, this also matched
# apps/api/src/waqil_api/data/, silently excluding the vendored DAC sizing
# catalog and Oracle SKU catalog the app needs to run at all.
/data/
artifacts/
uploads/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Editor and local workspace state
.vscode/
.idea/
.claude/

# Personal QA harnesses that drive a running instance and the live database
qa/

# Python
.venv/
.uv-cache/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Node
node_modules/
.pnpm-store/
.next/
*.tsbuildinfo

# Build output and generated archives
dist/
build/
coverage/
metis-export*.zip
metis-offline-bundle*.zip

# Design exploration assets (not shipped; the product mark lives in metis-mark.tsx)
design/
brand/

# Internal phase-tracking design notes, kept local
docs/tool-factory-v2-plan.md

# Native app build products
apps/mac/.build/
dist/
